<div class="card">
   <div class="row">
      {{#each students }}
      <div class="col-md-3">
         <a href="/student/detail/{{_id}}">
            <img class="card-img-top" src="{{ image }}" width="200" height="300">
         </a>
         <div class="card-body">
            <h5 class="card-title">{{ name }}</h5>
         </div>
      </div>
      {{/each}}
   </div>
</div>